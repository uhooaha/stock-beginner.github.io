# OS / Editor junk (Mac/Windows/Linux)
.DS_Store
Thumbs.db
*.swp
*.swo
*.swn
*.swc
*.tmp
*~

# Ruby & Bundler
.bundle/
vendor/
Gemfile.lock if you want to lock dependencies for local dev only
# ↑ Gemfile.lock은 협업 방식에 따라 제외할 수도, 포함할 수도 있습니다.

# Jekyll build artifacts
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata

# Node modules (if using npm/yarn)
node_modules/

# Optional: if you have other local scripts or caches
coverage/
